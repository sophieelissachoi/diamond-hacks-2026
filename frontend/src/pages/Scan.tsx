import { Box, Heading } from "@chakra-ui/react";

export default function ScanRecipe() {
	return (
		<Box p={6}>
			<Heading size="lg">Scan and summarize recipes</Heading>

			<Box
				as="form"
				mt={4}
			>
				{/* we’ll add inputs later */}
			</Box>
		</Box>
	);
}
